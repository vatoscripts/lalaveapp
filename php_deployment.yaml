apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /source
      containers:
      - name: php
        #image: hirenkavad/laravel-k8
        image: kiyange26773/sourceapp2:latest
        volumeMounts:
        - name: dir
          mountPath: /source
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /source
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/source/app"
